export interface Product {
    name: string;
    description: string;
    price: number;
    category: string;
    image: string;
}

export interface Seller {
    fullName:string,
    email:string,
    shopName:string,
    description:string,
    image:string
}